const { pool } = require('../db/mysql');

const STATEMENTS = [
  `CREATE TABLE IF NOT EXISTS DOCUMENTOS (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    EMPNIT VARCHAR(50) NOT NULL,
    NUMERO INT NOT NULL,
    FECHA DATE NOT NULL,
    NIT VARCHAR(20) NOT NULL,
    NOMBRE VARCHAR(300) NOT NULL,
    DIRECCION VARCHAR(500) NULL,
    EMAIL VARCHAR(150) NULL,
    CODPACIENTE INT NULL,
    CONCRE VARCHAR(10) NOT NULL DEFAULT 'CONTADO',
    TIPO_DTE VARCHAR(10) NOT NULL DEFAULT 'FACT',
    IMPORTE DECIMAL(12, 2) NOT NULL DEFAULT 0,
    SERIE_FEL VARCHAR(50) NULL,
    NUMERO_FEL VARCHAR(50) NULL,
    FECHA_FEL DATETIME NULL,
    UUID VARCHAR(80) NULL,
    ESTADO VARCHAR(20) NOT NULL DEFAULT 'PENDIENTE',
    IDENTIFICADOR VARCHAR(120) NULL,
    XML_CERTIFICADO LONGTEXT NULL,
    MENSAJE VARCHAR(1000) NULL,
    OBS VARCHAR(500) NULL,
    UNIQUE KEY uk_documentos_empnit_numero (EMPNIT, NUMERO),
    INDEX idx_documentos_empnit_fecha (EMPNIT, FECHA),
    INDEX idx_documentos_estado (EMPNIT, ESTADO),
    INDEX idx_documentos_uuid (UUID)
  )`,
  `CREATE TABLE IF NOT EXISTS DOCPRODUCTOS (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    EMPNIT VARCHAR(50) NOT NULL,
    CODDOC INT NOT NULL,
    CODPROD VARCHAR(150) NOT NULL,
    DESPROD VARCHAR(300) NOT NULL,
    CANTIDAD DECIMAL(12, 2) NOT NULL DEFAULT 1,
    COSTO DECIMAL(12, 2) NOT NULL DEFAULT 0,
    PRECIO DECIMAL(12, 2) NOT NULL DEFAULT 0,
    TOTAL DECIMAL(12, 2) NOT NULL DEFAULT 0,
    ORDER_ID INT NULL,
    INDEX idx_docproductos_doc (EMPNIT, CODDOC)
  )`,
  `CREATE TABLE IF NOT EXISTS credencialesFEL (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    EMPNIT VARCHAR(50) NOT NULL,
    NIT_EMISOR VARCHAR(20) NOT NULL,
    NOMBRE_EMISOR VARCHAR(300) NOT NULL,
    NOMBRE_COMERCIAL VARCHAR(300) NULL,
    DIRECCION VARCHAR(500) NULL,
    CODIGO_POSTAL VARCHAR(10) NULL,
    MUNICIPIO VARCHAR(100) NULL,
    DEPARTAMENTO VARCHAR(100) NULL,
    PAIS VARCHAR(5) NOT NULL DEFAULT 'GT',
    CODIGO_ESTABLECIMIENTO VARCHAR(10) NOT NULL DEFAULT '1',
    CORREO_COPIA VARCHAR(150) NULL,
    USUARIO_FIRMA VARCHAR(120) NULL,
    LLAVE_FIRMA VARCHAR(255) NULL,
    USUARIO_API VARCHAR(120) NULL,
    LLAVE_API VARCHAR(255) NULL,
    URL_FIRMA VARCHAR(500) NULL,
    URL_CERTIFICACION VARCHAR(500) NULL,
    URL_ANULACION VARCHAR(500) NULL,
    URL_PROCESO_UNIFICADO VARCHAR(500) NULL,
    URL_CONSULTA_NIT VARCHAR(500) NULL,
    TIPO_CONTRIBUYENTE VARCHAR(10) NOT NULL DEFAULT 'GEN',
    TIPO_FRASE INT NOT NULL DEFAULT 1,
    CODIGO_ESCENARIO INT NOT NULL DEFAULT 1,
    USAR_UNIFICADO VARCHAR(2) NOT NULL DEFAULT 'SI',
    UNIQUE KEY uk_credencialesfel_empnit (EMPNIT)
  )`,
];

async function ensureFelTables() {
  for (const sql of STATEMENTS) {
    await pool.query(sql);
  }
}

module.exports = { ensureFelTables };
